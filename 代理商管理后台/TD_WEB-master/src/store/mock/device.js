export const deviceList = {
  totalCount: 40,
  pageSize: 15,
  pageId: 1,
  'rows|20': [{
    'deviceId|+1': 0,
    a: '@string',
    b: /(0)|(1)/,
    c: '@datetime("T")',
    d: '@city(true)',
    e: '@string',
    f: '@string',
    g: '@string',
    h: '@string',
    i: '@name',
    j: '@datetime("T")',
  }],
};

